Judgements for a DIFFERENT sample of mushoku16 lines, not the 139-row bundle.

The reply self-describes as "batch 2 of 10" and uses an `ANSWER` field rather
than `speaker`; only 2 of its 40 ids appear in our bundle and none in the
147-row gold. It is real data for lines nobody has otherwise labelled, kept
here rather than deleted, but it cannot be ingested into this fixture and its
presence in the _wide folder made ingest_judgements refuse the whole book.
