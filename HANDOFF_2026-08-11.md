# Handoff — 2026-08-11, 00:31 central

State at time of writing. Everything below was read off the machine, not
recalled. Where a number is an estimate it says so.

---

## 1. What is running right now

Four chain scripts are alive. **Two of them are doing no work** (see §3).

| chain | started | state |
|---|---|---|
| `run_chains/unseen_books.sh` | 2026-08-10 ~17:00 | working — mushoku23, chunk 102/120 |
| `run_chains/decontaminate_library.sh` | 2026-08-10 ~19:35 | **starved**, zero work done |
| `run_chains/moss_vs_lora.sh` | 2026-08-10 ~21:45 | **starved**, zero work done |
| `run_chains/pdnc_generation.sh` | — | FINISHED 01:21Z |

### The one doing work

`unseen_books.sh` generates four light novels in sequence:

```
mushoku18  -> done
grimgar06  -> FAILED at chunk 25/70 after 77 retries
mushoku23  -> running, chunk 102/120, ~43 min left
arc4_volume10wn -> queued, 947,730 chars ~= 172 chunks ~= 6.9 h (estimate)
```

So generation continues for roughly **7.5 more hours** from now.

### How to check on it

```bash
cd /home/fakemitch/pinokio/api/alexandria-audiobook2.git
tail -3 ab_test_runtime/logs/unseen_mushoku23.log      # current book
grep -E "^=== " ab_test_runtime/logs/unseen_chain.log  # per-book progress
ps -eo etime,cmd | grep run_chains | grep -v grep      # what is alive
```

---

## 2. Results from the finished PDNC run

`pdnc_generation.sh` completed. Four books, **two written**.

| book | chunks | retries | written |
|---|---|---|---|
| TheSignOfTheFour | 41/41 | 7 | yes |
| TheAwakening | 49/49 | 11 | yes (1,943 entries) |
| AlicesAdventuresInWonderland | 25/25 | 22 | **no** — final gate |
| DaisyMiller *(chain's own run)* | 1/22 | 14 | **no** — died at chunk 2 |

Both failures have since been diagnosed and fixed. See §4.

Outputs: `ab_test_runtime/pdnc_generated/*.json`
Per-book logs: `ab_test_runtime/logs/pdnc_<book>.log`

Alice's completed entries survive in its checkpoint even though the book was
never written — 919 entries, recoverable:

```
ab_test_runtime/pdnc_generated/AlicesAdventuresInWonderland.json.generation_checkpoint.json
```

---

## 3. The starvation problem — READ BEFORE TOUCHING ANYTHING

`decontaminate_library.sh` (5h) and `moss_vs_lora.sh` (2h50m) have produced
**nothing**. Both sit in this loop:

```bash
while pgrep -f "generate_script.py " >/dev/null 2>&1 \
   || pgrep -f "three_pass_generate.py" >/dev/null 2>&1; do
    echo "waiting for the GPU $(date -u +%H:%M:%SZ)"
    sleep 300
done
```

Generation has run continuously since before they started, so they have never
advanced past the wait.

**Do not delete that wait.** It is a real safety net, not a bug —
`moss_vs_lora.sh` loads an 8B model (MOSS-TTSD) and would OOM against a live
generation. Removing it trades a slow run for a crashed one.

The actual defect is *fairness*: `gpu_job.sh` provides a `flock`-based queue,
but only jobs that opt in respect it. The generation chains never call it, so
a polite chain waits forever behind impolite ones. The fix is to route every
chain through `gpu_job.sh` so there is one queue.

**That fix was NOT applied, deliberately.** All four chain scripts are
currently executing, and bash reads a script lazily as it runs — editing a
running `.sh` can corrupt its execution mid-flight. Apply it only once the
chains have exited.

Two options when someone picks this up:

- Wait ~7.5 h for `arc4_volume10wn`, then apply the `gpu_job.sh` routing.
- Drop `arc4_volume10wn` from the queue now so the two starved chains start in
  about 45 minutes. This changes queued work, so it needs a decision.

---

## 4. Code changes today

### MERGED — #289, duplicate classification

Finished books were being discarded over duplicates the repair already knew how
to fix. Two causes, neither a defect in the book:

1. **`\w` includes underscore.** Project Gutenberg marks italics with it. The
   source reads `explain _myself_`, the model correctly drops the markup and
   writes `myself`, and the two never matched. **23 of the 28 public-domain
   novels use the convention**, so all of them were being measured against a
   source whose emphasised words could not be matched.
2. **Entries are not contiguous source spans.** `"Explain yourself!"` and
   Alice's reply have `said Alice` between them, so the joined block scores
   zero occurrences while each line scores one — read as "the model invented
   this".

Alice after the fix: 919 → 914 entries, unresolved 0, blocking 0, **gate
passes**. Its recall was never the problem (98.1% token, 96.7% trigram against
a 90% floor).

Pinned by tests in `app/test_duplicate_classification.py`: invented repetition
still deletes nothing, and grimgar03's genuinely repeated title is still kept.

### OPEN — #288, source health preflight

`preflight_source()` in `app/repair_source_encoding.py` checks a source for
known damage before the first LLM call and repairs what it can, in memory. The
user's file on disk is never rewritten.

Wired into both generation paths from one definition, with an agreement test —
a capability living on one path only is the defect shape that has now recurred
four times in this repo.

Corpus scan: **19 of 36 books flagged, 19 fully repairable, no healthy book
flagged.**

Proven end to end: Daisy Miller previously died at chunk 2 of 22; with the
preflight restoring 468 contractions it generated **22/22, 910 entries**.

CI failed once and is fixed: the preflight had been placed *before*
`repair_lossy_replacements` on the three-pass path, so it repaired the U+FFFD
characters itself and the existing repairer reported zero fixes. Text was
identical either way, but the run's self-report went to zero, so the ordering
was wrong rather than the tests. Now runs after. 1502 tests pass.

**Repetition threshold is calibrated from measurement, not intuition:**

| | repetitions |
|---|---|
| runs that demonstrably broke generation (index18) | 25 and 42 |
| legitimate typography, max observed | 13 |

Two earlier versions of that detector flagged healthy books — a stutter
(`I-I-I-I-`), a Japanese scene break (`※　※　※`), a spaced ellipsis. Flagging a
working book is worse than not checking.

---

## 5. How to run things (exact commands)

Repo root: `/home/fakemitch/pinokio/api/alexandria-audiobook2.git`
Python: `app/env/bin/python` — run generation from **inside `app/`**.

### Generate one book

```bash
cd /home/fakemitch/pinokio/api/alexandria-audiobook2.git/app
env/bin/python -u generate_script.py \
    ../ab_test_runtime/pdnc/data/DaisyMiller/novel_text.txt \
    --output ../ab_test_runtime/pdnc_generated/DaisyMiller.json
```

### Generate under the GPU lock (queues behind other lock-aware jobs)

```bash
cd /home/fakemitch/pinokio/api/alexandria-audiobook2.git
export GPU_LOCK=$HOME/.alexandria_gpu.lock
export GPU_QLOG=$PWD/ab_test_runtime/logs/gpu_jobq.log
./gpu_job.sh <job_name> timeout 21600 \
    $PWD/app/env/bin/python -u app/generate_script.py <input.txt> --output <out.json>
```

### Check a source before generating

```bash
cd /home/fakemitch/pinokio/api/alexandria-audiobook2.git/app
env/bin/python -c "
import sys; sys.path.insert(0,'.')
from repair_source_encoding import preflight_source
t=open('<book.txt>',encoding='utf-8').read()
r=preflight_source(t)
[print(m) for m in r['messages']]
print('healthy:', r['healthy'])"
```

### Tests

```bash
cd /home/fakemitch/pinokio/api/alexandria-audiobook2.git/app
env/bin/python -m unittest discover -s . -p "test_*.py"     # 1502 tests, ~20s
env/bin/python update_test_inventory.py                      # AFTER staging new tests
```

`update_test_inventory.py` only counts **git-tracked** modules, so run it after
`git add`, never before, or `test_inventory` fails.

---

## 6. Open items, in priority order

1. **VRAM guard decision** — blocks alias dedupe. 625 lines, 16% of index18,
   have one character under two labels. Needs a human decision.
2. **Starvation fix** (§3) — apply `gpu_job.sh` routing once chains exit.
3. **GOALS.md consolidated update** — 3.1 MET citing the regression run, 5.3's
   third book (index18 −19.2), corpus-wide quote calibration, and the preflight
   work. Several updates have accumulated; one pass is better than five.
4. **grimgar06 failed** at chunk 25/70 with 77 retries. It ran on pre-commit
   code, so no preflight. Whether that is connected is **untested** — its
   flagged trap was a 7-repetition stutter, which the calibrated threshold
   deliberately ignores, so do not assume a link.
5. **Alice rerun** — the fix is merged but Alice has not been regenerated. Its
   919 entries sit in the checkpoint; a rerun should now write the book.

---

## 7. Things I got wrong today — do not re-derive these

- **The GPU lock does not prevent contention.** I claimed queuing a job behind
  `gpu_job.sh` would stop it competing. It only serialises jobs that opt in;
  three books generated simultaneously against one endpoint anyway.
- **"The model elides quote-free narration"** — proposed after seeing chunk 10
  drop one contiguous region seven times. Measured it: **7 of 10 dropped
  regions had MORE quotes than their chunk baseline**, not fewer. Dead. What
  survives is only that the losses were contiguous and positional.
- **A waiter that greps `Error:`** matches the benign LM Studio startup warning
  that appears near the top of every generation log. It fires instantly and
  looks like a result. Scope the search to text after the marker you care
  about. This has now cost time three times.
- **`source_occurrences == 0` does not mean invented.** It meant "not
  contiguous" or "differs by an underscore". See §4.

---

## 8. Key paths

```
app/repair_source_encoding.py   preflight_source, check_source_health, repair
app/script_preflight.py         duplicate detection, occurrence counting
app/generate_script.py          single-pass generation, final gate (~line 1490)
app/three_pass_generate.py      three-pass path, prepare_source_text (~line 1330)
app/script_repair.py            build_deterministic_repair
gpu_job.sh                      flock-based GPU serialisation
run_chains/                     overnight chain scripts
ab_test_runtime/logs/           all run logs
ab_test_runtime/pdnc_generated/ PDNC outputs + checkpoints + quality manifests
```

Logs to read first when something looks wrong:
`ab_test_runtime/logs/<chain>_<book>.log`, then the book's
`*.generation_quality.json` — the manifest records the gate's actual verdict
and the repairs it computed, which is usually the answer.
