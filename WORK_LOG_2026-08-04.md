# What happened after the review response — 2026-08-04

Continues `REVIEW_RESPONSE_2026-08-04.md`, which closed the nine findings from
`REVIEW_2026-08-04.md`. This covers everything since: two further defects the
review's *observations* led to, three corrections to claims made earlier in the
day, and the re-measured numbers that replace contaminated ones.

Commits: `7db17b1` (generation guard), `4322028` (roster-free rule, seed test,
training checkpoints), `f28d61f` (repository-relative paths, two reusing
harnesses), `517ef65` (instruction listening artifact). Suite: **1,197 run, 0
failures.**

---

## Summary

| # | What | Severity | Status |
| --- | --- | --- | --- |
| A | Six harnesses ignored the TTS return value and scored stale audio | High | fixed, `7db17b1` |
| B | Finding 4 was mitigated, not fixed — no roster meant no rule | Medium | fixed, `4322028` |
| C | `gpu_job.sh` on the cloud box was still the unchecked-`flock` version | High | deployed + verified |
| D | `job_shippable.sh` called a script that does not exist on that box | Medium | fixed |
| E | Distill training died twice at the same step; my diagnosis was wrong | Medium | re-diagnosed, running |
| F | Seed bug inflated the headline non-prose numbers | — | re-measured |

---

## A — Stale audio scored as fresh

`tts.py` has **26 `return False` sites**. **Zero of six** experiment harnesses
checked that boolean; all tested `os.path.exists(wav)` instead. With a WAV left
at that path by an earlier run, a *failed* generation was counted as a success
and stale audio was scored as fresh. Invisible to the harness, artifact looks
complete, number wrong in a direction nobody checks.

Fixed with **one shared renderer**, not six patches — six independent copies is
precisely how six of them came to share the bug. `experiments/generation.py`
deletes the output *first*, dispatches through `voice_category` as production
does, checks the boolean, checks the file is non-empty, and **raises**. A caller
forgetting to check a returned bool is how this started; an exception cannot be
ignored by omission.

`ok is False`, not `not ok` — some paths return `None` on success, and treating
that as failure would discard good audio.

`nonprose_split_test` needed more than a swap: that comparison is *paired*, so a
failure now drops the segment from **both** arms and records it. Letting the
surviving arm through is exactly finding 5's asymmetry.

**Two more files, and I was wrong about them.** I flagged `clone_vs_lora.py`
and `voice_data_saturation.py` as carrying the same unchecked-return pattern.
They do not — both check the boolean. Their actual defect is different and is
described in the addendum below: they *reused* audio from earlier runs instead
of regenerating. Fixed in `f28d61f`.

---

## B — Finding 4, from mitigated to fixed

The review's closing note was right: the intervening-name rule required a
caller-supplied roster, so with no roster the original false attribution simply
returned. A rule that only works when the caller already knows the answer is
barely a rule.

Capitalised, non-sentence-initial tokens that are neither the target nor a
common capitalised non-word are now treated as intervening characters.

```
gender_from_narration("Subaru watched Emilia raise her hand." * 3, "Subaru")
  before:  ('female',  'medium', 0 masc / 3 fem)
  after:   ('unknown', 'unknown', 0 / 0)

same text, "Emilia"  ->  ('female', 'medium', 0 / 3)     # true referent kept
```

The guess is **asymmetric on purpose**. A place name wrongly blocked costs one
piece of evidence out of hundreds; a character name wrongly missed costs a
character the wrong voice.

**Verified on real data rather than assumed** — over-blocking would have been a
silent regression. Across 180,974 characters of narration:

| character | with roster | roster-free |
| --- | --- | --- |
| SUBARU | male, 83/10 | male, **88**/10 |
| EMILIA | female, 0/9 | female, 0/9 |
| REINHARD | male, 14/1 | male, 14/1 |
| ROM | male, 9/0 | male, 9/0 |
| SATELLA | female, 3/12 | female, 3/12 |
| FELT | unknown, 9/14 | unknown, 9/14 |

Every verdict agrees. No evidence lost.

The module remains **NOT WIRED INTO PRODUCTION**. That has not changed and is
still a product decision rather than a defect fix.

---

## C, D — The cloud box was running different code than the repo

Both jobs queued overnight had already failed by the time I looked.

**`gpu_job.sh` on the instance was still the pre-fix version** — bare `flock 9`
with no gate, the exact defect of review finding 1. The fix had been committed
locally and never deployed. So the guard that finding 1 was about had been
"fixed" for hours while the machine that actually runs GPU jobs did not have it.

Now deployed and **verified on the box**, not just locally: with a `flock` that
returns 73, the wrapped command did not run, exit was 4, and the queue log
records `LOCK_FAILED` with no `START` and no `OK`.

**`job_shippable.sh` called `~/ensure_llama_server.sh`; the file on that box is
named `ensure_server.sh`.** Exit 127 under `set -e`. That is the whole reason
the shippable-mixed eval failed — not the `EnvironmentCaptureError` from the
earlier attempt, which was a different run.

It also ended with `pkill -f llama-server`, which is exactly the caller-kills
ownership inversion `ensure_llama_server.sh` exists to remove. Removed.

`distill_train.py` was byte-identical between box and repo, so the patch was
applied by exact string replacement and **verified by SHA-256 against the local
file** rather than by reading it back.

Worth noting: the failing traceback showed `/home/fakemitch/...` paths *on the
cloud box*. That is review improvement 2 — the hard-coded path that only works
there through an unrecorded symlink — visible in the wild.

---

## E — The training OOM, re-diagnosed

Earlier today I attributed the first distill-training death to a concurrent
`llama-server` stealing VRAM, and cited it as the cost of the `gpu_job.sh` race.

The second attempt died at **step 218/1250, the same step**, with the GPU
otherwise idle, 14.72 GiB free, on a 1.13 GiB request. Same step twice with
memory available is fragmentation, not contention.

The lock race was real and worth fixing. **It was not the cause of this OOM,**
and saying it was is the kind of mechanism-without-reproduction the review
called out.

Now running with `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` (the remedy
the error message itself names), `--save_steps 100`, and a retry policy decided
once and applied identically on every attempt: on non-zero exit, resume from the
latest checkpoint if one exists, otherwise stop. Previously `save_strategy` was
`"epoch"`, so both deaths left nothing to resume from and cost ~41 minutes each.

---

## F — Numbers that changed once generation was seeded

`generate_lora_voice` never read the seed, so six comparisons were run on
audio where each line was an independent draw of the voice. All are being
re-run rather than defended. Completed so far:

| measurement | contaminated | seeded |
| --- | --- | --- |
| non-prose failures | 21/25, WER 56.65% | **11/25, WER 29.40%** |
| prose failures | 0/25 | **0/25, WER 0.89%** |
| splitting front matter | 7/8 still failed | **8/8 both arms**, split marginally worse |
| bullet-list fix | 0/2 → 3/3 | **3/3, 0 errors** |
| instruct per-line / per-char / none | — | **0.20% / 0.20% / 0.41% WER, 0 failures all arms** |
| within-voice pitch spread | ~103 Hz | **32.4 Hz median** over 6 adapters (48 Hz was one adapter, above median) |
| same text, same seed, ×3 | — | **150.048 Hz, identical to 12 dp** |

**The seed bug roughly halved the non-prose failure rate.** The gap survives and
is large — +28.5 WER points at matched length, with symbols already normalised
away — but the number I reported was inflated.

**Splitting is now definitively not the remedy.** 8/8 fail either way. Two of
those segments contained **one item**, could not be split at all, and failed
anyway. That localises the mechanism to the item, not the blob, and is why
`nonprose_mechanism.py` exists.

**Instructions do not change content.** One word of difference across 18
segments in three arms. Whether they change *delivery* is a listening question
the WER harness cannot answer, and the artifact says so rather than implying a
verdict.

**Pitch: the withdrawal stands but for a different reason.** Half the ~103 Hz
instability was the seed bug. The remaining 48 Hz is prosody, and it does not go
away. The scene-aware allocator targeted 17 Hz — a third of what one voice
covers by itself. Not "pitch is unstable" but "voices must be further apart than
the assumed threshold". `pitch_separation.py` measures how much further.

---

## Two process failures of my own, recorded

**I mutated the live `tts.py` to prove the new seed test could fail,** while an
experiment queue was running against it. The command timed out and left the file
in the bugged state for about two minutes. Restored from git and verified; the
running process had already imported the module, so no output was affected — but
that was luck, not design. Redone on a copy in the scratchpad.

**I collided with my own earlier queue.** A queue launched before the context
compaction was still working through the seeded re-runs. My new one waited for
the wrong process, declared the card free, and started generating alongside it.
Killed mine by PID and chained properly on the driver's PID. Every "noise floor"
in this repo's history has turned out to be another job on the same GPU.

**A claim I had made twice was wrong.** I said the three sibling generation
paths all read the seed. `generate_clone_voice` and `generate_custom_voice`
contain no seed logic at all — they are dispatchers, and the seeding lives in the
`_local_*` implementations one level down. The code was correct; my description
of it was not. The new test asserts the layering so the next reader does not
repeat it, and its first draft failed against correct code for exactly this
reason.

---

## Running now

**Local**, one card, serialised:

| job | state |
| --- | --- |
| `pitch_separation` | running, 3 of 6 adapters |
| `casting_ab_audio` (seeded) | queued |
| `nonprose_mechanism` | queued |

**Cloud**, A6000, serialised through `gpu_job.sh`:

| job | state |
| --- | --- |
| PDNC-only distill training | running, step ~67/1250 at 10.9 s/it |
| `distill_eval --tag pdnc_only` | chained |
| `shippable-mixed` LoRA serving eval | chained, runs regardless of training |

**The step to watch is 218** — where it died twice. It now checkpoints every 100
steps and auto-resumes, so a third death costs minutes rather than the run. But a
third death at 218 would mean the fragmentation diagnosis is also wrong, and that
is a reason to look again rather than to retry.

---

---

## Addendum — queue items 1-3, approved and done

**Item 1 corrected on contact.** I described `clone_vs_lora.py` and
`voice_data_saturation.py` as carrying the same unchecked-return defect as the
six routed through `render()`. Both actually **do** check the boolean. Their
defect is a different one I had not named: `if not os.path.exists(path)`
skipped generation when a file was already present, so a WAV from an earlier
run entered the comparison — and on an unseeded path that is a different draw
of the voice, charged to clone-vs-lora or to the sample count. `not good` also
treats a None-on-success return as failure, the opposite of `render()`'s
deliberate `is False`. Both now always regenerate through `render()` at a fixed
seed. `clone_vs_lora` had hard-coded `seed: -1` on the clone arm and passed
none on the lora arm.

**Item 2, 77 files.** `REPO` is now derived from `__file__`. Five files did not
import `os` above the assignment and would have raised at import time; the
migration checked for that and skipped them rather than writing something
broken. All 89 scripts were then executed for the `REPO` expression with
`__file__` bound as Python binds it — every one resolves to the same path as
before — and the five hand-fixed files were imported for real.

**Item 3 built, and its first draft was wrong.** Selecting lines on "has a
non-neutral instruction" returned four copyright and publisher-URL lines: the
register that fails 44% of the time on its own. A delivery difference would
have been buried under an unrelated failure mode. Now filtered through
`prose_vs_nonprose.classify`, reused rather than reimplemented.

---

## `pitch_separation` — result

| adapter | declared | measured | within-voice spread |
| --- | ---: | ---: | ---: |
| warm_bass_50s_m_fantasy | 94.8 | 85.6 | 31.9 |
| warm_baritone_30s_m_fantasy_1 | 120.2 | 132.2 | 35.1 |
| silky_baritone_45s_m | 131.1 | 133.4 | 32.9 |
| warm_tenor_25s_m_military | 148.0 | 141.6 | 31.1 |
| silky_alto_40s_f | 179.5 | 209.2 | 14.4 |
| husky_soprano_20s_f | 246.2 | 264.3 | 71.9 |

**Median within-voice spread is 32.4 Hz, not the 48 Hz measured this morning.**
That single adapter was above the median, so the figure I reported was on the
high side. The requirement is roughly **32 Hz**, against the allocator's
assumed 17.

**49.3% of the 2,775 adapter pairs clear 32 Hz** — so pitch is usable as a
**constraint** (never cast two voices closer than this) but not as a general
contrast objective.

**One caveat that limits that percentage, stated because it is load-bearing.**
It is computed from the manifest's *declared* `mean_f0`, and declared vs
measured has a mean absolute error of **12.9 Hz, worst 29.7 Hz** — comparable
to the requirement itself. So 49.3% is approximate, and using declared values
to pick voices 32 Hz apart can be wrong by most of that margin. Re-profiling
the pool on seeded audio would be needed before the constraint could be applied
by number rather than by ear.

---

## What is not queued, and why

**Recommended, no GPU needed:**

1. **Route `clone_vs_lora.py` and `voice_data_saturation.py` through `render()`.**
   Same unchecked-return defect as the six already fixed. Cheap, and leaving two
   copies of a bug that was just fixed in six places is how it comes back.
2. **Improvement 2 — repository-relative paths.** The hard-coded
   `/home/fakemitch/...` in `distill_train.py` and elsewhere is load-bearing on a
   disposable cloud box through a symlink recorded nowhere. It surfaced in a
   traceback today. This is the item most likely to break the next instance.

**Recommended, needs the card:**

3. **A listening artifact for `instruct_value`.** WER says the three arms are
   identical in content. The open question is delivery, and that needs a paired
   audio file, not another WER run. Small: 3 arms × a few lines.

**Deliberately not queued:**

- **More cloud jobs.** Nothing should be added behind a 3.5-hour training run
  whose survival is the open question. If step 218 falls, the queue is right; if
  it does not, anything queued behind it was wasted planning.
- **Review "Tests still needed" items 1–10.** Still largely unwritten. Item 10
  (deterministic local-LoRA generation) is now covered by `test_seed_plumbing.py`
  at the source level and by the recorded byte-identical waveform check
  end-to-end. Item 1 (lock behaviour under failure) is verified on both machines
  but is still not a committed test.
- **Re-running anything already re-run seeded.** Four of the six contaminated
  comparisons are done. The remaining two are in the local queue.
